// src/test/setup.js

// Adds all jest-dom matchers like toBeInTheDocument()
import '@testing-library/jest-dom';

// // Mock the image import so tests don’t fail with "Cannot find module"
// vi.mock('/media/images/homeHero.png', () => ({
//     default: '/media/images/homeHero.png'
// }));
